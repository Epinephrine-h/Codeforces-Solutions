#include <iostream>
 
using namespace std;
 
void Answer()
{
    int testcase;
    cin >> testcase;
    while(testcase--){
        int t;
        cin >> t;
        if (t >= 1900)      cout << "Division 1";
        else if (t >= 1600)     cout << "Division 2";
        else if (t >= 1400)     cout << "Division 3";
        else    cout << "Division 4";
        cout << '
';
    }
}
 
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    Answer();
    return 0;
}