#include<iostream>
#include<unordered_set>
 
using namespace std;
 
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    unordered_set<int> s;
    for (int i = 0; i < 4; i++){
        int x;
        cin >> x;
        s.insert(x);
    }
    cout << 4 - s.size();
    return 0;
}