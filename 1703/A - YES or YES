#include<iostream>
#include<cctype>
using namespace std;
 
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int testcase;
    cin >> testcase;
    while(testcase--){
        string s;   cin >> s;
        for (int i = 0; i < s.size(); i++)     s[i] = toupper(s[i]);
        if (s == "YES")     cout << "YES";
        else    cout << "NO";
        cout << '
';
    }
    return 0;
}